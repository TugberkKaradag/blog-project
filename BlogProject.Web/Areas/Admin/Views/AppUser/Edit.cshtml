@model AppUser
@using BlogProject.Model.Enums;
@{
    ViewData["Title"] = "Edit";
}

<div class="row">
    <div class="col-md-12">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger">@Model.UserName</div>
            <div class="form-group">
                <select asp-for="Statu" asp-items="@Html.GetEnumSelectList<Statu>()"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Güncelle" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

